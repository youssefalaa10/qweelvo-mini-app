apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: qweelvo-mini-app
  namespace: core
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: qweelvo-mini-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
